  // private readonly buyerRepository: Repository<Buyer> = AppDataSource.getRepository(Buyer)
  // private readonly productRepository: Repository<Product> = AppDataSource.getRepository(Product)
  // private readonly orderRepository: Repository<Order> = AppDataSource.getRepository(Order)
  // private readonly orderDetailRepository: Repository<OrderDetail> = AppDataSource.getRepository(OrderDetail)
  // private readonly cartRepository: Repository<Cart> = AppDataSource.getRepository(Cart)
  // private readonly paymentRepository: Repository<Payment> = AppDataSource.getRepository(Payment)
  // private readonly shipmentRepository: Repository<Shipment> = AppDataSource.getRepository(Shipment)
  // private readonly deliveryRepository: Repository<Delivery> = AppDataSource.getRepository(Delivery)
  // private readonly addressRepository: Repository<Address> = AppDataSource.getRepository(Address)
  // private readonly countryRepository: Repository<Country> = AppDataSource.getRepository(Country)
  // private readonly stateRepository: Repository<State> = AppDataSource.getRepository(State)
  // private readonly cityRepository: Repository<City> = AppDataSource.getRepository(City)
  // private readonly orderStatusRepository: Repository<OrderStatus> = AppDataSource.getRepository(OrderStatus)

  // export default new class BuyerOrderService {
    
  //   createOrder(req: Request, res: Response): Promise<Response> {
  //     try {
        
  //     } catch (error) {
        
  //     }
  //   }
  // }